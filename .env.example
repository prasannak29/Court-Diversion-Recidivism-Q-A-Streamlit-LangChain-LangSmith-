# --- OpenAI ---
OPENAI_API_KEY=sk-...

# --- LangSmith (optional but recommended) ---
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=ls-...
LANGCHAIN_PROJECT=court-diversion-app

# --- Vector DB path ---
REVIEWS_CHROMA_PATH=chroma_data/ALM

# --- Pricing (override if needed; $ per 1k tokens) ---
PRICE_INPUT_PER_1K=0.01
PRICE_OUTPUT_PER_1K=0.03
